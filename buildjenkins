pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                bat cmd archiveArtifacts artifacts: '**/target/*.jar', fingerprint: true
                   
            }
                  }
    
    }
}
  
